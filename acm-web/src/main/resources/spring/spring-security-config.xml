<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
    xmlns="http://www.springframework.org/schema/security"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <!-- Configure Spring Security. -->    

    <!-- The login page itself, along with the JavaScript and images that appear on the login page, do not have to be protected. -->
    <http
        security="none"
        pattern="/login" />
    <http
        security="none"
        pattern="/login.html" />
    <http
        security="none"
        pattern="/login.jsp" />
    <http
        security="none"
        pattern="/assets/css/login.css" />
    <http
        security="none"
        pattern="/sso/loggedout" />
    <http
        security="none"
        pattern="/resources/**" />
    <http
        security="none"
        pattern="/branding/**" />
    <http
        security="none"
        pattern="/external/**" />
    <!-- All lib are public, no security needed -->
    <http
        security="none"
        pattern="/lib/**" />
          
    <!-- import filter chains from external configuration file -->
    <beans:import resource="file:${user.home}/.arkcase/acm/spring/spring-security-config-auth.xml" />
    
</beans:beans>
