<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
            http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.2.xsd">

    <context:property-placeholder
        properties-ref="userNotificationProperties"
        ignore-unresolvable="true"
        local-override="false"
        order="987691"/>

    <bean id="userNotificationProperties"
          class="net.unicon.springframework.addons.properties.ReloadablePropertiesFactoryBean" >
        <!-- note: must leave "file:" at the start of the file name for spring
        to be able to read the file; otherwise it will try to read from the
        classpath -->
        <property name="location" value="file:${user.home}/.acm/user-notification.properties"/>
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="localOverride" value="false"/>
        <property name="properties">
            <props>
                <prop key="user.notification.batch.run">true</prop>
                <prop key="user.notification.batch.frequency">3600000</prop>
                <prop key="user.notification.batch.size">50</prop>
            </props>
        </property>
    </bean>
    
    <bean id="userNotificationPropertiesConfigurer" class="net.unicon.springframework.addons.properties.ReloadingPropertyPlaceholderConfigurer">
		<property name="properties" ref="userNotificationProperties"/>
	</bean>

    
    <task:scheduler id="userNotificationTaskScheduler" pool-size="1"/>
    <task:scheduled-tasks scheduler="userNotificationTaskScheduler">
        <task:scheduled
                ref="notificationService"
                method="run"
                fixed-delay="${user.notification.batch.frequency}"/>
    </task:scheduled-tasks>
    
    <bean id="notificationService" class="com.armedia.acm.service.usernotification.service.UserNotificationServiceImpl">
    	<property name="batchRun" value="${user.notification.batch.run}" />
    	<property name="batchSize" value="${user.notification.batch.size}" />
    	<property name="propertyFileManager">
            <bean class="com.armedia.acm.files.propertymanager.PropertyFileManager"/>
        </property>
        <property name="userNotificationPropertyFileLocation" value="${user.home}/.acm/user-notification.properties"/>
        <property name="assignRule" ref="assignRule" />
        <property name="unassignRule" ref="unassignRule" />
        <property name="acmAssignmentDao" ref="acmAssignmentDao" />
    </bean>

</beans>