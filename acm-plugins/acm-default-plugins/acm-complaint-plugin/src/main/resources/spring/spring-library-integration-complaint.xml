<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:int="http://www.springframework.org/schema/integration"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd">

    <int:channel id="complaintFilteredMailChannel"/>

    <bean id="complaintMailFilter" class="com.armedia.acm.services.email.filter.AcmObjectPatternMailFilter">
        <constructor-arg index="0" value="${complaint.email.regex.complaint_number}"/>
        <constructor-arg index="1" value="${complaint.email.regex.object_type}"/>
    </bean>

    <bean id="complaintMailHandler" class="com.armedia.acm.services.email.handler.AcmObjectMailHandler">
        <constructor-arg index="0" ref="acmComplaintDao"/>
        <property name="mailDirectory" value="${casefile.email.folder.relative.path}"/>
        <property name="enabled" value="${complaint.email.handler.enabled}"/>
        <property name="objectIdRegexPattern" value="${complaint.email.regex.complaint_number}"/>
        <property name="acmFolderService" ref="acmFolderService"/>
        <property name="ecmFileService" ref="ecmFileService"/>
        <property name="auditPropertyEntityAdapter" ref="auditPropertyEntityAdapter"/>
    </bean>

    <int:service-activator input-channel="complaintFilteredMailChannel" ref="complaintMailHandler" method="handle"/>

    <int:chain input-channel="mailChannelComplaint" output-channel="complaintFilteredMailChannel">
        <int:filter ref="complaintMailFilter" throw-exception-on-rejection="false" method="accept"/>
    </int:chain>


    <!-- Create new Complaint from incoming mail -->
    <bean id="newComplaintMailFilter" class="com.armedia.acm.services.email.filter.ComplaintFilePatternMailFilter">
        <constructor-arg index="0" value=""/>
        <constructor-arg index="1" value="${complaint.email.regex.object_type}"/>
    </bean>

    <bean id="newComplaintMailHandler" class="com.armedia.acm.plugins.complaint.email.NewComplaintMailHandler"
          parent="complaintMailHandler">
        <property name="objectTypeRegexPattern" value="${complaint.email.regex.object_type}"/>
        <property name="saveComplaintTransaction" ref="saveComplaintTransaction"/>
        <property name="lookupDao" ref="lookupDao"/>
        <property name="emailReceiverPropertiesResource" value="file:${user.home}/.arkcase/acm/acmEmailReceiver.properties"/>
    </bean>

    <int:service-activator input-channel="newComplaintFilteredMailChannel" ref="newComplaintMailHandler" method="handle"/>

    <int:chain input-channel="mailChannelComplaint" output-channel="newComplaintFilteredMailChannel">
        <int:filter ref="newComplaintMailFilter" throw-exception-on-rejection="false" method="accept"/>
    </int:chain>
</beans>