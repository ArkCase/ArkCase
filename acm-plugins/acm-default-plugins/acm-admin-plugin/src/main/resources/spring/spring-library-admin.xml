<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd">

    <!-- keeping this bean for now even though we are not using any properties yet.  -->
    <bean id="adminPluginProperties"
          class="org.springframework.beans.factory.config.PropertiesFactoryBean" >
        <!-- note: must leave "file:" at the start of the file name for spring
        to be able to read the file; otherwise it will try to read from the
        classpath -->
        <property name="location" value="file:${user.home}/.acm/adminPlugin.properties"/>
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="localOverride" value="false"/>
        <property name="properties">
            <props>
            </props>
        </property>
    </bean>

    
    <bean id="adminPlugin" class="com.armedia.acm.pluginmanager.model.AcmPlugin">
        <property name="pluginName" value="Admin Plugin"/>
        <property name="pluginProperties" ref="adminPluginProperties"/>
    </bean>


    <bean id="labelManagementService" class="com.armedia.acm.plugins.admin.service.LabelManagementService">
        <property name="modulesLocation" value="#{ systemProperties ['user.home'] }/.acm-ui/custom/modules/" />
        <property name="moduleConfigLocation" value="#{ systemProperties ['user.home'] }/.acm-ui/custom/modules/%s/module_config/config.json" />
        <property name="moduleResourcesLocation" value="#{ systemProperties ['user.home'] }/.acm-ui/custom/modules/%s/module_config/resources/%s.json" />
        <property name="customResourceFile" value="%s.%s.json" />
        <property name="customResourcesLocation" value="#{ systemProperties ['user.home'] }/.acm/resources/custom/" />
        <property name="resourceFile" value="%s.%s.json" />
        <property name="resourcesLocation" value="#{ systemProperties ['user.home'] }/.acm/resources/resources/" />
        <property name="settingsFileLocation" value="#{ systemProperties ['user.home'] }/.acm/resources/settings/settings.json" />
        <property name="languages">
            <list>
                <value>en</value>
            </list>
        </property>
    </bean>

</beans>